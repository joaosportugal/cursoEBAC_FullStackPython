function multiplicar(num1: number, num2: number): number {
    return num1 * num2
}

function saudar(nome: string): string {
    return `Olá ${nome}`
}

console.log(multiplicar(12, 5000));
console.log(saudar('tutor'));